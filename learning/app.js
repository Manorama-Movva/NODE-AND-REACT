//this file is for importing the exposes functions add div sub and mul
var add = require('./add');

console.log(add.addition(4,6))

var sub = require('./sub');

console.log(sub.subtraction(14,6))

var mult = require('./mul');

console.log(mult.multiply(4,6))

var div = require('./div');//div that in in braces is div.js it is file name

console.log(div .division(4,6)) // div here is same as variable name we declared above and division is the name we declared in div.js file